package com.switube.www.landmark2018test.adapter.callback;

public interface IAReplies {
    void handleClickLike(String like, int index);
    void handleFocus();
    void handleEdit(int index);
    void handleDelete(int index);
}
