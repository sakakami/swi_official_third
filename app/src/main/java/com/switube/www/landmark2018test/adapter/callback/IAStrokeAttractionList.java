package com.switube.www.landmark2018test.adapter.callback;

public interface IAStrokeAttractionList {
    void handleCollectPlace(int index, boolean isAdd);
    void handleRemoveTrip(int index);
    void handleSaveTrip(int index);
    void handlePhotoClicked(int index);
}
